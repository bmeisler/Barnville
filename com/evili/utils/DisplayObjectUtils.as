package com.evili.utils {	import flash.display.*;	public class DisplayObjectUtils {		// Remove all of the children in a container		public static function removeAllChildren(container:DisplayObjectContainer):void {			// Because the numChildren value changes after every time 			// you remove a child, save the original value so you can 			// count correctly			var count:int=container.numChildren;			// Loop over the children in the container and remove them			for (var i:int=0; i<count; i++) {				container.removeChildAt(0);			}		}		public static function removeAllNestedChildren(container:DisplayObjectContainer):void {			// Because the numChildren value changes after every time 			// you remove a child, save the original value so you can 			// count correctly			var count:int=container.numChildren;			// Loop over the children in the container and remove them			for (var i:int=0; i<count; i++) {				var tempContainer=container.getChildAt(0);				if (tempContainer (typeof DisplayObject)){					if (tempContainer.numChildren>0) {						DisplayObjectUtils.removeAllNestedChildren(tempContainer);					}				}				container.removeChildAt(0);				tempContainer = null;			}		}	}}